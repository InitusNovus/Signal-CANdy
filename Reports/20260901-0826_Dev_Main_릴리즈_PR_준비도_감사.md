# dev → main 릴리즈 PR 준비도 감사

## 📝 작업 요약

`origin/dev`(`d603373`)를 `origin/main`(`cba4225`)으로 승격할 수 있는지 릴리즈 체크리스트, GitHub Actions, 버전·문서 정합성, 대표 CLI/코드 생성 동작, 보안 의존성 기준으로 감사했다.

판정은 **FAIL — PR은 기술적으로 생성 가능하지만 현재 상태로 release-ready/merge-ready가 아니다.**

- `dev`는 `main`보다 157 commits ahead, 0 behind이며 merge-tree 생성은 성공했다.
- 변경 범위는 156 files, 약 32,510 insertions / 429 deletions이다.
- 기존 AOT DBC→C99와 신규 SCIMG validate/build/inspect/diff 대표 동작은 통과했다.
- 그러나 현재 HEAD CI 실패, 이미 배포된 `0.4.0` 버전 재사용, README/ROADMAP 불일치, Scriban 보안 경고가 릴리즈를 차단한다.

## 🛠 변경 상세

- 생성: `Reports/20260901-0826_Dev_Main_릴리즈_PR_준비도_감사.md`
- 제품 소스·테스트·설계 문서는 수정하지 않았다.
- 별도 review worktree `C:/Users/initusnovus/Desktop/Workspace/Signal-CANdy-release-review`를 detached `origin/dev`로 생성하고 잠갔다.
- 활성 ROADMAP 항목을 완료한 작업은 없으므로 `Plans/ROADMAP.md` 체크박스 변경은 없다.

### Release blockers

1. **현재 HEAD CI 실패**
   - Run: https://github.com/InitusNovus/Signal-CANdy/actions/runs/33091010386
   - `build-test`: PASS, `lint`: PASS, `hardening`: FAIL.
   - `scripts/hardening.ps1`가 tool project를 `--no-restore`로 빌드하지만 독립 hardening job은 해당 project를 restore하지 않아 `NETSDK1004`가 발생한다.

2. **릴리즈 버전 충돌**
   - GitHub/NuGet 최신 릴리즈는 이미 `0.4.0`.
   - Core/Facade fsproj, `Api.version()`, README EN/KO, NuGet README도 모두 `0.4.0`.
   - runtime schema 신규 기능을 동일 package version으로 배포할 수 없다. 다음 버전 결정이 필요하며 SemVer상 `0.5.0` 계열이 자연스럽다.

3. **릴리즈 문서 불일치**
   - README EN/KO와 NuGet README가 여전히 AOT DBC→C99 제품만 설명하며 SCIMG, project manifest, activation/hot-swap, inspect/map/diff를 설명하지 않는다.
   - `Plans/ROADMAP.md`는 이미 CLOSED인 #17과 #19–#25를 아직 architecture/child 분할 전 상태로 기술한다.
   - `src/Generator/Program.fs`의 배너도 `v0.3.2`로 남아 있다.

4. **보안·zero-warning gate 실패**
   - `Scriban 6.2.1` 직접 의존성으로 release build에 28개 NU1902/NU1903/NU1904 경고가 발생했다.
   - 보안 리뷰는 여러 high/critical advisories를 확인했고 `Scriban >= 7.2.2`를 요구했다.
   - 이는 AGENTS pre-release checklist의 build 0 warnings 조건을 위반한다.

5. **diff hygiene 실패**
   - `git diff --check origin/main...origin/dev`:
     - `src/Signal.CANdy/Library.fs:67`
     - `src/Signal.CANdy/Library.fs:161`
   - 두 위치에 trailing whitespace가 있다.

## ✅ 테스트 결과

- GitHub Actions HEAD `d603373`
  - build-test: PASS
  - lint/Fantomas: PASS
  - hardening: FAIL (`NETSDK1004`, missing hardening tool assets)
- Hands-on QA:
  - CLI help: PASS
  - invalid CLI grammar/exit code: PASS
  - legacy DBC→C99 generation: PASS
  - generated C build/run: PASS
  - SCIMG project validate/build/inspect/diff: PASS
  - 임시 QA 디렉터리 삭제 및 부재 확인: PASS
- Git:
  - `merge-tree --write-tree origin/main origin/dev`: PASS
  - ahead/behind: `157/0`
  - merge conflict evidence: 없음
  - `git diff --check`: FAIL (trailing whitespace 2건)
- Review worktree tracked status: clean.
- 실제 하드웨어 접근과 HIL 재실행은 하지 않았다.

## ⏭ 다음 계획

1. hardening job에서 tool project를 restore하거나, script의 CI 경로가 clean checkout에서 self-contained restore하도록 수정한다.
2. 다음 릴리즈 버전을 결정한다. 권장 후보는 `0.5.0-alpha.1` 검증 후 `0.5.0`.
3. Core/Facade fsproj, `Api.fs`, README EN/KO, NuGet README, generator banner를 동일 버전으로 맞춘다.
4. README EN/KO와 NuGet 문서에 runtime-schema 제품 기능과 실제 CLI 흐름을 반영한다.
5. `Plans/ROADMAP.md`를 #17 및 #19–#25 CLOSED 상태와 다음 IR/AOT 통합 과제로 갱신한다.
6. Scriban을 안전 버전으로 올리고 AOT golden/codegen 회귀를 검증한다.
7. trailing whitespace 2건을 제거한다.
8. Release build/test/Fantomas/hardening/pack을 clean checkout에서 통과시키고 새 HEAD GitHub CI green을 확인한 뒤 `dev → main` PR을 연다.
