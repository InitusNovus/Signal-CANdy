# >64 Valid Bitmask 설계문서 완성 보고서

## 📝 작업 요약

`>64` 시그널 valid bitmask fallback 정책에 대한 **설계 전용** 작업을 완료했다. 코드 구현은 수행하지 않았으며, 기존 baseline(152/152 테스트, 빌드 0 warning/error)은 변경 없이 유지된다.

### 세션 개요

| 항목 | 내용 |
|------|------|
| 세션 목적 | >64 시그널 valid bitmask fallback 정책 설계 (코드 구현 없음) |
| 완성물 | `Plans/design_valid_gt64_bitmask.md` (820줄, 8섹션 + 부록 2개) |
| 심사 결과 | Momus OKAY — "The plan is executable as written" |
| Baseline | 코드 변경 없음 → 152/152 pass, 0 warnings 유지 |
| 관계 | Phase 1–3 (ROADMAP 감사, 종결, Plans/ 이관)은 커밋 `d23802e`에서 완료됨 |

설계 과정에서 다음 전문 에이전트가 투입되었다:
- **explore** (×2): `Codegen.fs` 내 valid bitmask 관련 9개 touch point 전수 조사, Scriban 템플릿 아키텍처 검증
- **librarian**: C99 portable >64-bit bitmask 접근법 조사
- **metis**: gap analysis — 7개 질문, 9개 guardrail, 5개 scope lock, 6개 edge case, 11개 AC 도출
- **momus**: 설계문서 심사 → **OKAY**

사용자 설계 결정 4건이 모두 수렴되어 문서에 반영되었다.

---

## 🛠 변경 상세

### 신규 파일

| 파일 | 라인 수 | 설명 |
|------|---------|------|
| `Plans/design_valid_gt64_bitmask.md` | 820 | >64 valid bitmask fallback 설계문서 (8섹션 + 부록 2개) |
| `Reports/20260315_2031_Valid_GT64_설계문서_완성.md` | — | 본 세션 보고서 |

### 수정 파일

없음. 설계 전용 세션이므로 소스 코드, 테스트, 템플릿에 대한 변경 없음.

### 설계문서 구성

| 섹션 | 내용 요약 |
|------|-----------|
| §1 Current State | `Codegen.fs` 9개 touch point 전수 분석 (L447–1064) |
| §2 Problem Definition | >64 signal `UnsupportedFeature` hard error 실제 영향 범위 |
| §3 Policy Alternatives | 4개 대안 비교 (uint128, GCC extension, bitset struct, **byte-array 채택**) |
| §4 Recommended Approach | `uint8_t valid_bytes[N]` + `sc_valid_set/test/clear` 헬퍼 매크로 |
| §5 MVP Scope | 8개 Codegen 수정(M1–M8), 2개 템플릿 변경(T-UTILS, T-HDR) |
| §6 Test Strategy | TDD RED→GREEN→REFACTOR, 11개 AC, 7-phase atomic commit |
| §7 Risk Assessment | 10개 리스크(R1–R10), 10개 guardrail(G1–G10) |
| §8 Sisyphus Prompt | 자기 완결형 구현 프롬프트 (zero-ambiguity) |
| Appendix A | 설계 결정 로그 (Q1–Q4) |
| Appendix B | 증거 소스 목록 |

### 사용자 설계 결정 4건

| # | 질문 | 결정 |
|---|------|------|
| Q1 | >64 메시지 헤더에 `sc_utils.h` include 여부? | 자동 조건부 include |
| Q2 | API 경계 (scalar→array) 전환 기준? | 65-signal에서 전환 (불연속 수용) |
| Q3 | 최대 시그널 상한? | 1024 |
| Q4 | 배열 크기 표현 방식? | `#define MSG_VALID_BYTES N` + struct 내 literal array 항상 emit |

---

## ✅ 테스트 결과

설계 전용 세션이므로 코드 변경이 없어 baseline 그대로 유지:

- **`dotnet build --configuration Release --nologo`**: ✅ 0 warnings / 0 errors
- **`dotnet test --configuration Release -v minimal --nologo`**: ✅ 152/152 pass
  - Signal.CANdy.Core.Tests: 125/125
  - Generator.Tests: 27/27

### 설계문서 검증

| 검증 항목 | 결과 |
|-----------|------|
| 자체 검토: 모든 ID 일관성 (M1–M8, T-UTILS/T-HDR, AC1–AC11, G1–G10, R1–R10, EC1–EC6, SC1–SC5) | ✅ Dangling reference 없음 |
| 자체 검토: 7-phase commit 전략 내 파일 경로 실존 확인 | ✅ 확인 |
| Momus 심사 | ✅ **OKAY** — "All cited repository files and code locations exist and are relevant" |

---

## ⏭ 다음 계획

### 즉시 실행 가능: >64 Valid Bitmask 구현

설계문서 §8의 Sisyphus 구현 프롬프트가 자기 완결형으로 준비되어 있다:

```
/start-work design_valid_gt64_bitmask
```

**TDD 실행 순서:**
1. RED: 6개 테스트 그룹 작성 (AC1–AC11 대응)
2. GREEN: M1→M2→M3→M4→M5→M6→M7→M8 순 구현 (의존관계 순)
3. REFACTOR: 7-phase atomic commit 완료

**7-phase commit 전략:**
- Phase 1: RED 테스트 추가 (`test(valid-gt64): add RED test group 1–6`)
- Phase 2–5: M1–M8 구현 단계별 commit
- Phase 6: T-UTILS/T-HDR 템플릿 변경
- Phase 7: REFACTOR + 문서 정합성 확인

### 별도 스코프 (이번 설계와 무관)

- ROADMAP item #2: Oracle reference decoder incompatibility 대응 (별도 세션)
- 기타 `Plans/ROADMAP.md` 미완료 항목
