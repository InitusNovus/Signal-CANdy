## 📝 작업 요약
- `Ir.fs`의 신규 필수 필드(`Message.CrcCounterMode`, `Signal.CrcMeta`, `Signal.CounterMeta`) 반영 누락으로 발생한 FS0764 빌드 오류를 테스트 코드에서 모두 보정했다.
- 테스트 로직/단언/구조 변경 없이, 누락된 레코드 리터럴 필드에 `None`만 추가해 컴파일 정합성을 복구했다.

## 🛠 변경 상세
- 수정 파일: `tests/Signal.CANdy.Core.Tests/CodegenTests.fs`
  - `Message` 리터럴 누락 지점들에 `CrcCounterMode = None` 추가.
  - `Signal` 리터럴 누락 지점들에 `CrcMeta = None`, `CounterMeta = None` 추가.
  - `mkMuxSwitch`, `mkBranchSignal` 헬퍼의 `Signal` 리터럴에도 동일 필드 추가.
- 수정 파일: `tests/Signal.CANdy.Core.Tests/EdgeCaseTests.fs`
  - `Signal` 리터럴 누락 지점에 `CrcMeta = None`, `CounterMeta = None` 추가.
  - `Message` 리터럴 누락 지점에 `CrcCounterMode = None` 추가.
- 수정 파일: `tests/Signal.CANdy.Core.Tests/FacadeTests.fs`
  - `mkBranchSignal`의 `Signal` 리터럴에 `CrcMeta = None`, `CounterMeta = None` 추가.
- 비수정 확인: `src/` 하위 프로덕션 코드는 변경하지 않음.

## ✅ 테스트 결과
- LSP 진단
  - `tests/Signal.CANdy.Core.Tests/CodegenTests.fs`: No diagnostics found
  - `tests/Signal.CANdy.Core.Tests/EdgeCaseTests.fs`: No diagnostics found
  - `tests/Signal.CANdy.Core.Tests/FacadeTests.fs`: No diagnostics found
- 빌드 검증
  - 실행: `dotnet build --configuration Release --nologo`
  - 결과: 성공 (exit 0)
  - 경고: 0
  - 오류: 0

## ⏭ 다음 계획
- 현재 세션 범위(FS0764 테스트 레코드 필드 누락 수정)는 완료.
- 후속으로 필요 시 `dotnet test --configuration Release -v minimal --nologo` 전체 테스트 실행해 런타임 회귀 여부를 추가 확인할 수 있다.
