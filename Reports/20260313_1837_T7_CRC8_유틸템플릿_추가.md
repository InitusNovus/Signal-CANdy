## 📝 작업 요약

- T7 수행: `utils` Scriban 템플릿에 CRC-8 SAE J1850 / CRC-8 8H2F 선언·구현 블록을 조건부로 추가하고, `Codegen.fs` Utils 모델에 `has_crc_j1850=false`, `has_crc_8h2f=false` 기본값을 주입했다.
- 비CRC 경로의 golden parity를 유지하기 위해 헤더 조건부 블록의 줄바꿈 배치를 조정해 기존 출력과 byte-identical 상태를 보존했다.

## 🛠 변경 상세

- `templates/utils.h.scriban`
  - `has_crc_j1850` / `has_crc_8h2f` Scriban 조건부 선언 추가.
  - 두 플래그가 모두 `false`일 때 추가 공백 줄이 생기지 않도록 조건부 블록을 재배치했다.
  - CRC 선언이 활성화될 때만 `size_t`가 필요한 점을 반영할 수 있도록 헤더 쪽 조건부 include 구조를 마련했다.
- `templates/utils.c.scriban`
  - CRC-8 SAE J1850 lookup table + `sc_crc8_sae_j1850()` 구현 추가.
  - CRC-8 8H2F lookup table + `sc_crc8_8h2f()` 구현 추가.
  - 두 구현 모두 동일한 Scriban boolean으로 가드되어 기본 생성 결과는 변하지 않는다.
- `src/Signal.CANdy.Core/Codegen.fs`
  - `Utils.utilsHContent` 모델에 `"has_crc_j1850", box false`, `"has_crc_8h2f", box false` 추가.
  - `Utils.utilsCContent` 모델에 동일한 두 boolean 엔트리 추가.
- `.sisyphus/notepads/l2bc-crc-counter/learnings.md`
  - T7에서 확인한 golden parity 유지 패턴과 CRC 템플릿 wiring 메모를 append 했다.

## ✅ 테스트 결과

- `lsp_diagnostics` on `src/Signal.CANdy.Core/Codegen.fs` → diagnostics 0.
- `dotnet build --configuration Release --nologo` → 성공, 0 errors / 2 warnings.
  - 경고 위치: `src/Signal.CANdy.CLI/Program.fs:342`, `src/Generator/Program.fs:128`
  - 내용: `ByteRangeExceedsDlc` 패턴 매치 누락 경고(FS0025), 기존 경고 유지.
- `dotnet test --configuration Release -v minimal --nologo` → 116/116 PASS.
  - `Signal.CANdy.Core.Tests`: 89 PASS
  - `Generator.Tests`: 27 PASS
- 중간 검증:
  - 처음 시도에서 golden tests가 추가 blank line / unconditional include 때문에 실패함을 확인.
  - 템플릿 줄바꿈 구조를 조정한 뒤 `CodegenTests` 재실행으로 parity 회복을 확인했다.

## ⏭ 다음 계획

- 다음 세션은 T8/T10 범위에서 CRC 유틸 사용 지점을 메시지 템플릿과 IR 기반 bool 계산으로 연결해야 한다.
- `ROADMAP.md`의 `L-2b`는 아직 전체 완료 상태가 아니므로 이번 세션에서는 체크박스를 갱신하지 않았다.
