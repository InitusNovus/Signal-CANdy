# 📝 작업 요약

- T6 목표에 맞춰 `Config.validate`에 CRC/Counter 설정 검증을 추가했다.
- `crc_counter` 블록 존재 시 모드/알고리즘/폭/카운터 모듈러스/시그널명 규칙을 검증하고, 첫 번째 오류를 구조화된 `ValidationError`로 반환하도록 구현했다.

# 🛠 변경 상세

- 수정: `src/Signal.CANdy.Core/Config.fs`
  - `validCrcCounterModes`, `builtinAlgorithmWidths` 상수 추가.
  - `validateCrcCounter` private helper 추가.
    - `crc_counter_check=false` + `crc_counter` 존재 시 `ConfigConflict` 반환.
    - `mode` 허용값(`validate`, `passthrough`, `fail_fast`) 검증.
    - CRC 알고리즘 인지 실패 시 `UnknownAlgorithm` 반환.
    - 인지된 알고리즘 폭이 8이 아니면 `ConfigConflict` 반환(MVP 제한).
    - Counter `modulus < 2` 시 `InvalidModulus` 반환.
    - CRC/Counter 시그널명이 비어 있으면 `InvalidValue` 반환.
  - 기존 기본 검증 체인 이후 `validateCrcCounter`를 호출하도록 `validate` 확장(시그니처 유지).
- 수정(기록):
  - `.sisyphus/notepads/l2bc-crc-counter/learnings.md`
  - `.sisyphus/notepads/l2bc-crc-counter/issues.md`
  - `.sisyphus/notepads/l2bc-crc-counter/decisions.md`

# ✅ 테스트 결과

- LSP 진단: `src/Signal.CANdy.Core/Config.fs` 진단 0건.
- 빌드: `dotnet build --configuration Release --nologo` 성공 (오류 0, 기존 FS0025 경고 4 유지).
- 테스트: `dotnet test --configuration Release -v minimal --nologo` 성공.
  - `Signal.CANdy.Core.Tests`: 89 통과
  - `Generator.Tests`: 27 통과
  - 총 116/116 통과

# ⏭ 다음 계획

- T6 후속인 Config 기반 IR 적용/검증 단계(T9/T10)에서 `SignalNotFound`, `ByteRangeExceedsDlc` 등 DBC 연동 검증을 추가한다.
- 신규 `ValidationError` 경로를 Facade/CLI 매핑 분기에서 누락 없이 처리하도록 후속 정리한다.
