# ROADMAP 정합성 상세 감사 보고서 (정정 포함)

## 📝 작업 요약
- 목적: `ROADMAP.md`의 체크 완료 항목(`[x]`)과 현재 저장소 실제 구현/테스트/문서 상태의 정합성을 evidence-first로 점검.
- 범위: `ROADMAP.md` 전 섹션(Critical/High/Medium/Low/Tracking) + `src/`, `tests/`, `templates/`, `.github/workflows/ci.yml`, `README.md`, `README.ko.md`, `Reports/`, `tests/oracle/`.
- 결론: 핵심 기능 구현 자체는 대체로 ROADMAP과 일치하나, **문구 범위 과장 1건(L-3a)**, **문서 시점 불일치 3건(L-2 설명, Tracking oracle 근거, 고정 pass 수치)**, **가이드 문서 의존성 표 불일치 1건(AGENTS Key Dependencies)** 확인.

---

## 🛠 변경 상세
- 생성 파일: `Reports/20260315_1648_ROADMAP_정합성_상세감사_정정포함.md`
- 코드 변경: 없음 (분석 전용)
- 실행 검증:
  - `dotnet build --configuration Release --nologo` → 경고 0, 오류 0
  - `dotnet test --configuration Release -v minimal --nologo` → 152/152 pass (Core 125 + Generator 27)
  - `fantomas --check src/ tests/` → exit 0
  - `dotnet run --project src/Signal.CANdy.CLI -- -d examples/crc_test.dbc -o gen -c examples/config_crc_t16.yaml -t`
  - 생성 코드 확인: `gen/src/message_1.c`에 CRC validate/insert + counter check 함수 생성 확인
- 체크박스 집계(`ROADMAP.md`):
  - 전체 `[x]` 68개, `[ ]` 0개
  - 섹션별: Critical 10 / High 16 / Medium 14 / Low 14 / Tracking 13

### 정합성 판정 매트릭스 (핵심)
1. **[High] L-3a 문구 범위 과장**
   - ROADMAP claim: `ROADMAP.md:123` — "`uint64_t` 또는 배열 기반 valid 필드 자동 선택"
   - 구현 근거:
     - `src/Signal.CANdy.Core/Codegen.fs:492` — 33개 초과 mux 신호 시 `uint64_t` 선택
     - `src/Signal.CANdy.Core/Codegen.fs:1058` — 64개 초과는 `UnsupportedFeature`
     - `tests/Signal.CANdy.Core.Tests/CodegenTests.fs:1617` — 65-signal mux에서 `UnsupportedFeature` 테스트
   - 판정: **부분 일치(문구 과장)**
   - 조치 제안: L-3a를 "33~64: uint64_t, >64: 미지원"으로 명시하고 배열 기반은 backlog로 분리.

2. **[High] L-2 완료 상태 vs README 설명 시점 불일치**
   - ROADMAP claim: `ROADMAP.md:118`, `ROADMAP.md:119` 완료
   - 구현 근거:
     - `src/Signal.CANdy.Core/Codegen.fs:809` — validate 모드 decode CRC 검증 생성
     - `src/Signal.CANdy.Core/Codegen.fs:836` — validate 모드 encode CRC 삽입 생성
     - `src/Signal.CANdy.Core/Codegen.fs:855` — counter check 함수 생성
     - `gen/src/message_1.c:25`, `gen/src/message_1.c:43`, `gen/src/message_1.c:47` — 실제 생성 결과 확인
     - `tests/Signal.CANdy.Core.Tests/CodegenTests.fs:678`, `tests/Signal.CANdy.Core.Tests/CodegenTests.fs:728` — CRC-8 validate 테스트
   - 충돌 문서:
     - `README.md:679`, `README.md:680`, `README.md:702`
   - 판정: **구현 완료(제한 범위) + README 서술 지연**
   - 조치 제안: README EN/KO를 현 상태(명시적 metadata + validate/passthrough/fail_fast + CRC-8 only)로 동기화.

3. **[High] Tracking source-of-truth 내부 시점 불일치**
   - ROADMAP claim: `ROADMAP.md:140`에서 `tests/oracle/ORACLE_RESULTS.md`를 상위 근거로 사용
   - 상충 근거:
     - `tests/oracle/ORACLE_RESULTS.md:60`, `tests/oracle/ORACLE_RESULTS.md:136` — 일부 항목을 권고/후속 형태로 유지
     - `tests/oracle/CATEGORY_C_EXCEPTIONS.md:25`, `tests/oracle/CATEGORY_C_EXCEPTIONS.md:54` — 동일 항목 RESOLVED 표기
   - 판정: **문서 간 시점 불일치**
   - 조치 제안: ORACLE_RESULTS 갱신 또는 ROADMAP의 source-of-truth 우선순위 재정렬.

4. **[Medium] M-4b 완료 주장 vs AGENTS 의존성 표 불일치**
   - ROADMAP claim: `ROADMAP.md:99` — 의존성 표 Core 기준 갱신 완료
   - 근거:
     - `AGENTS.md:178` Key Dependencies 표에 Scriban 누락
     - `src/Signal.CANdy.Core/Signal.CANdy.Core.fsproj:37` — `Scriban` 실제 사용
   - 판정: **문서 정합성 불일치**
   - 조치 제안: AGENTS Key Dependencies를 실제 Core 의존성 기준으로 갱신.

5. **[Low] ROADMAP 내 고정 pass 수치의 현재 기준선과 시차**
   - ROADMAP 표기: `ROADMAP.md:59`(16/16), `ROADMAP.md:81`, `ROADMAP.md:88`(63/63)
   - 최신 기준선:
     - 본 세션 실측: `dotnet test` 152/152 pass
     - 참조: `Reports/20260313_2230_L2bc_Final_Verification.md:92` — 152/152 기록
   - 판정: **역사 수치로는 유효하나 현재 baseline으로 오해 소지**
   - 조치 제안: "완료 당시 수치" 라벨 부착 또는 최신 리포트 링크 중심으로 단순화.

### 주요 일치 항목 (정상)
- C-1 계열 예외 전파 회귀: `tests/Signal.CANdy.Core.Tests/DbcTests.fs:21`, `tests/Signal.CANdy.Core.Tests/DbcTests.fs:25`
- C-2/H-3 에러 타입 보존 + Facade 매핑:
  - `src/Signal.CANdy.Core/Errors.fs:31`
  - `src/Signal.CANdy.Core/Api.fs:33`
  - `src/Signal.CANdy/Library.fs:124`
  - `src/Signal.CANdy.CLI/Program.fs:336`
- H-1g CI Core 테스트 실행: `.github/workflows/ci.yml:33`
- L-4c CAN FD DLC 매핑: `templates/utils.c.scriban:4`, `templates/utils.c.scriban:6`, `templates/utils.c.scriban:11`
- L-4 CAN FD 테스트 근거:
  - `tests/Signal.CANdy.Core.Tests/CodegenTests.fs:438`
  - `tests/Signal.CANdy.Core.Tests/EdgeCaseTests.fs:148`
  - `tests/Generator.Tests/CodegenTests.fs:430`

### 정정 사항 (Patch-forward)
- 본 세션 중 생성된 `Reports/20260315_1645_ROADMAP_정합성_감사.md`에는
  - `.sisyphus` 및 `Analysis/` 디렉터리가 현재 트리에 없다는 서술이 포함되어 있으나,
  - 실제 점검 결과 `.sisyphus/*`, `Analysis/Codebase_Analysis.md` 모두 존재함을 확인했다.
- 정확한 현행 판단은 **본 보고서**를 기준으로 한다.

---

## ✅ 테스트 결과
- `dotnet build --configuration Release --nologo` ✅ (경고 0, 오류 0)
- `dotnet test --configuration Release -v minimal --nologo` ✅ (152/152 pass)
- `fantomas --check src/ tests/` ✅ (exit 0)
- `dotnet run --project src/Signal.CANdy.CLI -- -d examples/crc_test.dbc -o gen -c examples/config_crc_t16.yaml -t` ✅
- 생성물 점검(`gen/src/message_1.c`) ✅
  - CRC decode 검증 코드 존재
  - CRC encode 삽입 코드 존재
  - counter check 함수 존재

---

## ⏭ 다음 계획
- ROADMAP 문구 보정 우선순위:
  1) L-3a 범위 문구 축소(배열 fallback 미구현 명시)
  2) L-2 상태를 README EN/KO와 동기화
  3) Tracking source-of-truth의 기준 문서 우선순위 재정렬
- 운영 문서 정합성 보정:
  1) AGENTS Key Dependencies에 Scriban 반영
  2) ROADMAP의 고정 pass 수치에 "완료 당시" 라벨 추가
