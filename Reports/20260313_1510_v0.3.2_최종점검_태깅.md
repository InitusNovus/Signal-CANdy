# 20260313_1510 — v0.3.2 최종 점검 및 릴리즈 태그 생성

## 📝 작업 요약

PR 머지 완료 이후 `main` 기준으로 v0.3.2 릴리즈 직전 최종 점검을 한 번 더 수행했다. 빌드/테스트/포맷/버전 정합성 모두 이상 없음을 확인했고, 같은 기준 commit에 `v0.3.2` 릴리즈 태그를 생성해 원격에 푸시했다.

## 🛠 변경 상세

- 브랜치 동기화
  - `main` 체크아웃 및 `origin/main` fast-forward 동기화

- 최종 점검 항목
  - 버전 정합성 확인:
    - `src/Signal.CANdy.Core/Signal.CANdy.Core.fsproj` -> `0.3.2`
    - `src/Signal.CANdy/Signal.CANdy.fsproj` -> `0.3.2`
    - `src/Signal.CANdy.Core/Api.fs` -> `let version () = "0.3.2"`
    - `README.md`, `README.ko.md`, `src/Signal.CANdy.Core/README.NuGet.md`, `src/Signal.CANdy/README.NuGet.md` install 버전 `0.3.2`

- 태그 작업
  - `git tag -a v0.3.2 -m "v0.3.2"`
  - `git push origin v0.3.2`

## ✅ 테스트 결과

- `dotnet build --configuration Release --nologo`
  - 결과: 경고 0 / 오류 0 ✅

- `dotnet test --configuration Release -v minimal --nologo`
  - 결과: 106/106 통과 ✅
  - `Signal.CANdy.Core.Tests`: 79 passed
  - `Generator.Tests`: 27 passed

- `fantomas --check src/ tests/`
  - 결과: 통과 ✅

## ⏭ 다음 계획

1. GitHub Release workflow의 `v0.3.2` 실행 결과 확인
2. (필요 시) 릴리즈 노트 최종 검토
3. 릴리즈 완료 후 후속 패치/다음 마일스톤 작업으로 전환
